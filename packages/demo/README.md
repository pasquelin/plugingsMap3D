# @map3d/plugin-demo

Preuve vivante de la plateforme, jamais publié comme officiel. Voie A (markers procéduraux,
zéro réseau), 4 types de champ de config (auto-rendu hub + dev panel), `enrichBuilding` et
tags de provenance. Lance son exemple : `pnpm --filter @map3d/plugin-demo-example dev`.
